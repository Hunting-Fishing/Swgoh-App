{
  "name": "swgoh-live-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:gamedata": "node scripts/sync-gamedata.mjs --allow-stale && node scripts/repair-catalog.mjs --allow-stale",
    "prestart": "node --test",
    "start": "node scripts/sync-gamedata.mjs --allow-stale && node scripts/repair-catalog.mjs --allow-stale && node server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
